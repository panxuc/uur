# uur injects input through the kernel uinput device on Wayland.
# uaccess grants the active seat user access without a group.
KERNEL=="uinput", SUBSYSTEM=="misc", TAG+="uaccess", OPTIONS+="static_node=uinput"
